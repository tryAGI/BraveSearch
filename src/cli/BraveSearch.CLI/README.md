# BraveSearch.CLI

Command-line interface for the BraveSearch SDK generated with AutoSDK.

## Installation

```bash
dotnet tool install --global BraveSearch.CLI --prerelease
```

## Usage

```bash
brave-search --help
brave-search api --help
```